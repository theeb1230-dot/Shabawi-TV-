# Upstream provenance

- Upstream: `Ahmd3301/plyrio`
- Default branch: `main`
- Status: `EMPTY`
- Expected upstream blobs: `0`

The public upstream currently reports an empty repository. This metadata file exists only so the upstream has a durable destination representation and is not counted as an upstream blob.
